[project]
name = "gamehub"
version = "0.1.0"
description = "A local PyQt6 game library manager and launcher."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.5.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.0",
]

[tool.uv]
package = false
